#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 * @x: The parameter to be checked for integer input
 * Return: integer - absolute value
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);

	else
		return (-x);
}

