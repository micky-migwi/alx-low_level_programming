#include "main.h"

/**
 * _isdigit - function  checks for a digit (0 through 9).
 * @c: value to check.
 * Return: int.
 */

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)

	{

		return (1);

	}

	else

		return (0);

}

